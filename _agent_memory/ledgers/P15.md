# P15 - Probe diagnostics telemetry
Status: DONE

(A) DESIGN: add per-host `diagnostics` to JSON aggregating state_counts, reason_counts, reliability buckets
(reusing P14), plus derived notes (majority no-response => filtering/blackholing; low>high reliability).
(B) IMPL: output_json.cc second pass over all ports (nextPort state 0). Guarded NULL strings. <map> include.
(C) TEST: tests/improvements/P15/run.sh - mixed open/closed tallies; UDP single silent port => note (root).
(D) DOC: docs/improvements/P15.md + IMPROVEMENTS.md. Status DONE.
